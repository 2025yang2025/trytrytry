name: Auto Stock Scanner

on:
  schedule:
    # 1. 下午 15:43 執行（UTC 07:43）避開 30 分大塞車
    - cron: '43 7 * * 1-5'
    # 2. 早上 08:18 執行（UTC 00:18）避開 30 分大塞車
    - cron: '18 0 * * 1-5'
  workflow_dispatch: # 允許手動點擊按鈕立即執行

jobs:
  scan:
    runs-on: ubuntu-latest

    steps:
    # 1. 複製儲存庫程式碼
    - name: Checkout repository
      uses: actions/checkout@v4

    # 2. 安裝 Python 環境
    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.10'

    # 3. 安裝必要套件
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install pandas yfinance requests lxml

    # 4. 執行選股腳本 (自動根據時間判斷要選股還是要發提醒)
    - name: Run Stock Scanner
      env:
        TG_BOT_TOKEN: ${{ secrets.TG_BOT_TOKEN }}
        TG_CHAT_ID: ${{ secrets.TG_CHAT_ID }}
      run: python scanner.py

    # 5. 自動將結果提交回 GitHub 儲存庫 (確保 results.md 被更新)
    - name: Commit and Push results
      run: |
        git config --local user.email "github-actions[bot]@users.noreply.github.com"
        git config --local user.name "github-actions[bot]"
        git add results.md
        git commit -m "Auto-update stock selection results" || echo "No changes to commit"
        git push
